import { MyEventDirectiveDirective } from './my-event-directive.directive';

describe('MyEventDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyEventDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
