import { MyColorChangeDirective } from './my-color-change.directive';

describe('MyColorChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new MyColorChangeDirective();
    expect(directive).toBeTruthy();
  });
});
